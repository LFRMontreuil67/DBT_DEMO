version: 2

sources:
  - name: my_dbt_db
    schema: my_dbt_db
    description: E-commerce data for the shop

    tables:
      - name: raw_customers
        description: One record per person who has purchased one or more items
      - name: raw_orders
        description: One record per order placed by a customer
version: 2

models:
  - name: stg_customers
    description: Customer data with basic cleaning and transformation applied, one row per customer.
    columns:
      - name: customer_id
        description: The unique key for each customer.
        tests:
          - unique
          - not_null
      - name: name
        description: The full name of the customer.
        tests:
          - not_null
version: 2

models:
  - name: stg_orders
    description: Order data with basic cleaning and transformation applied.
    columns:
      - name: order_id
        description: The unique key for each order.
        tests:
          - unique
          - not_null
      - name: customer_id
        description: The foreign key referencing the customer who placed the order.
        tests:
          - not_null
          - relationships:
              to: ref('stg_customers')
              field: customer_id
      - name: ordered_at
        description: The timestamp when the order was placed.
        tests:
          - not_null
      - name: store_id
        description: "{{ doc('store_id_description') }}"
      - name: subtotal
        description: The order total before tax, in cents.
        tests:
          - not_null
      - name: tax_paid
        description: The amount of tax paid on the order, in cents.
        tests:
          - not_null
      - name: order_total
        description: The total amount paid for the order, including tax, in cents.
        tests:
          - not_null
{% docs store_id_description %}

This is the ID for the store.

It references the ID column from the stores table.

{% enddocs %}
